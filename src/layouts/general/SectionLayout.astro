---
interface Props {
	id: string;
	ariaLabelledby: string;
}

const { id, ariaLabelledby } = Astro.props;
---
<section
	id={id}
	class="mx-auto max-w-6xl space-y-10 px-4 py-20 sm:px-6 lg:px-8"
	aria-labelledby={ariaLabelledby}
>
	<slot />
</section>
