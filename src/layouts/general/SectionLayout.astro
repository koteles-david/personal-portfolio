---
interface Props {
	id: string;
	ariaLabelledby: string;
}

const { id, ariaLabelledby } = Astro.props;
---
<section
	id={id}
	class="mx-auto max-w-6xl px-4 py-20 sm:px-6 lg:px-8 text-center space-y-10"
	aria-labelledby={ariaLabelledby}
>
    <slot />
</section>