---
interface Props {
	projectTitle: string;
}

const { projectTitle } = Astro.props;
---

<span
	class="inline-flex rounded-md border border-stone-700 px-2 py-0.5 text-xs font-medium text-stone-500"
	aria-label={`${projectTitle} is work in progress`}
>
	In progress
</span>
